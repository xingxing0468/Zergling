name: Clang Format
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  clang-format-checking:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: ./clang_format_action.yml
      with:
        style: Google
        sources: "cpps/**/*.h, cpps/**/*.cpp"